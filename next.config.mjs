/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      loaders: {
        // Add any custom loaders here if needed
      },
    },
  },
};

export default nextConfig;
